{% if role in ['server-worker', 'worker'] %}
server: https://{{ hostvars[groups['servers'][0]].ansible_host }}:9345
token: {{ lookup('file', rke2_token_local) | trim }}
{% endif %}

node-name: {{ inventory_hostname }}
